# OpenRedius: clients are driven by the radius.nas table (read_clients in
# mods-available/sql), NOT this file (docs/06「NAS 客户端生命周期」).
# The stock image ships a built-in `localhost` client (secret testing123)
# that collides with the seeded 127.0.0.1/testing123-dev dev radtest client,
# so this override leaves zero static clients.
#
# Add real static clients here only if you intentionally bypass the NAS table.
