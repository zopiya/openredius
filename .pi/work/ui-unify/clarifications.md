# UI 统一重构 — 澄清记录

## 已确认（用户决策）

| # | 问题 | 结论 | 来源 |
|---|---|---|---|
| C-1 | 主色 | #1677ff（antd 默认蓝），放弃 #0071e3 | 用户确认 |
| C-2 | 侧边栏 | 保持深色（ProLayout 经典），统一到 antd 深色 token | 用户确认 |
| C-3 | 自定义 CSS 边界 | 接受极少量必要自定义（SVG 降级、端口网格、图表 tooltip） | 用户确认 |

## 假设（未逐项问用户，按「完全参考 antd」推导）

| # | 假设 | 依据 |
|---|---|---|
| A-1 | 文本色/边框/背景/状态色回归 antd 默认灰阶与语义色（#ff4d4f 错误、#faad14 警告、#52c41a 成功） | C-1「完全参考 antd」的自然延伸 |
| A-2 | 字体回归 antd 默认字体栈，放弃 SF Pro 特指 | 同上 |
| A-3 | 圆角回归 antd 默认（Card 8px，放弃 18px 大圆角） | 同上 |
| A-4 | 两字中文按钮自动加空格（autoInsertSpace 回归默认） | antd 规范行为 |
| A-5 | Menu 深色选中态用主色 #1677ff（Pro 风格） | ProLayout 默认 |
| A-6 | 文案（术语/语气）在阶段 6 统一，重构各阶段不主动改文案 | 避免 fidelity 文本漂移 |

## 待确认（落地中如遇分歧再问）

- 无。以上假设若与你预期不符，随时打断。

## 质量自检

- [ ] 每个页面「空态/错误态/加载态」是否都有对应状态并统一组件？[完整性] → 现有 Empty/Result/Skeleton 已统一，本次保持
- [ ] 颜色映射是否覆盖所有 20 种硬编码色值？[完备性] → spec.md 映射表覆盖全部高频值，低频（#fffbeb/#fefce8 等）随语义组件自然消解
- [ ] autoInsertSpace 回归的连带选择器改动是否穷尽？[完备性] → 任务 1.3 用 grep 穷举按钮文案，落地时以 grep 结果为准
- [ ] 验收标准「inline style ≤40」是否可度量？[清晰性] → 用 `grep -c "style={{"` 精确度量
