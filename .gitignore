node_modules
dist
*.local
.DS_Store
*.tsbuildinfo
.tmp
coverage

# 环境变量与密钥(模板 .env.example 入库)
.env

# Python 后端(uv + pytest)
backend/.venv
backend/.pytest_cache
backend/.ruff_cache
*.pyc

# 本项目 Node 依赖统一用 bun(bun.lock);npm 锁文件不入库
package-lock.json

# 部署备份产物
deploy/backups

# pi session exports(TUI /export 产物)——个人本地文件，不入库
*.jsonl

# backend dev database
backend/openredius-dev.db
backend/*.db

# generated RADIUS dev certs (deploy/freeradius/certs/gen.sh / entrypoint fallback)
deploy/freeradius/certs/*.pem
deploy/freeradius/certs/*.key
deploy/freeradius/certs/*.srl
deploy/freeradius/certs/*.csr
