# OpenRedius Ansible 依赖 collections
# 安装:ansible-galaxy install -r requirements.yml
collections:
  - name: community.docker
    version: ">=3.10,<5.0"
  - name: community.general
    version: ">=9.0,<11.0"
  - name: ansible.posix
    version: ">=1.5,<3.0"
