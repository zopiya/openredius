---
# 部署 / 更新(幂等)
- name: OpenRedius 部署
  hosts: openredius
  gather_facts: true
  roles:
    - role: certs
      tags: [certs]
    - role: deploy
      tags: [deploy]
    - role: post_deploy
      tags: [verify]
